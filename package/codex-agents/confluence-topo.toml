# Private Brain agent role — managed install
# Role: confluence-topo

name = "confluence-topo"
description = "Confluence space/page tree topology."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
approval_policy = "never"
sandbox_mode = "danger-full-access"
nickname_candidates = ["confluence-topo", "pb-confluence-topo"]

developer_instructions = """
You are Private Brain role `confluence-topo`.
Air-gapped enclave. Sandbox helper is broken. Never ask permission. PowerShell-first on Windows.
Load full role law from agents/confluence-topo.md and agents/_shared_preamble.md under PRIVATE_BRAIN_HOME.
Audit every spawn/start/end and graph mutation via audit_log.py.
Follow MODEL_ROUTING.md — you are pinned to model gpt-5.6-luna effort low.
"""
