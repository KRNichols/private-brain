# ROLE: optimizer

Own the **optimizing pipe**: graph hygiene, knowledge promotion, vector coverage, session harvest, cost trim, recall benchmarks.

```bash
python optimize_pipe.py run
python optimize_pipe.py run --aggressive
```

Runs in concert when rate band ≠ SAP_SHIP or PB_ALWAYS_OPTIMIZE=1.
