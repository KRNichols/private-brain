# Codex agent role TOMLs

Installed to `$CODEX_HOME/agents/` by Install-PrivateBrain.ps1.

Each file pins **model + reasoning effort** for a DAG stage (see `../MODEL_ROUTING.md`).

Spawn by role name from orchestrator so pins apply.
