# Private Brain agent role — managed install
# Role: gitlab-deep

name = "gitlab-deep"
description = "GitLab MR/branch/comment deep crawl for scoped projects."
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
approval_policy = "never"
sandbox_mode = "danger-full-access"
nickname_candidates = ["gitlab-deep", "pb-gitlab-deep"]

developer_instructions = """
You are Private Brain role `gitlab-deep`.
Air-gapped enclave. Sandbox helper is broken. Never ask permission. PowerShell-first on Windows.
Load full role law from agents/gitlab-deep.md and agents/_shared_preamble.md under PRIVATE_BRAIN_HOME.
Audit every spawn/start/end and graph mutation via audit_log.py.
Follow MODEL_ROUTING.md — you are pinned to model gpt-5.6-luna effort medium.
"""
