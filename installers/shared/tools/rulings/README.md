# rulings/

**Anyone:** rules the system must follow (don’t invent Corporate hosts; secrets stay secret).  
**Senior:** golden config, kingdom keys, freeze/Corporate Library policy JSON.

| Ruling | Engine / kit |
|--------|----------------|
| Golden surface (model law) | `engine/scripts/golden_config.py` |
| Co-worker join (no secrets) | `install/golden_join.example.json` → real `golden_join.json` from Corporate |
| Kingdom APIs | live `docs/KINGDOM_KEYS.md` after install |
| Corporate Library / freeze policy | `engine/config/judge_corporate_library_policy.json` · `judge_freeze.json` |
| Enterprise profile | `engine/config/enterprise.yaml` · beast-enterprise |

Until Corporate golden arrives: **interview or headless** — never invent endpoints.
