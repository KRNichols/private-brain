# engine/

**Anyone:** you can ignore this folder.  
**Senior:** full sideload payload installed into `~/.codex/private-brain` (or `%USERPROFILE%\.codex\private-brain`).

```text
engine/
  scripts/      organism · orchestrate · enterprise · beastMode · …
  hooks/        session_start · user_prompt_submit · stop_validate
  visualizer/   GodsEye graph_gl.py
  config/       enterprise · model routing · judge policies
  agents/       skill cards
  docs/         deep mermaid / kingdom (mirrors)
```

Install (`tools/install/START`) copies/wires this into the live Codex home.  
The zip never ships `.brain` corpus data.
