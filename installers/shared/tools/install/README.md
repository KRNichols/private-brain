# install/

**Run once** (Mac ≡ Windows).

| OS | Command |
|----|---------|
| Windows | `.\START.ps1` |
| Mac | `./START.command` |

Also here: SETUP / UNINSTALL · `golden_join.example.json` (empty until Corporate golden).

After install: **open Codex and talk** — not a daily visit to this folder.
