# GodsEye — pygame only (same look on Mac and Windows).
# Install from Corporate Library / Protected Gateway when available.
# Not required for headless enterprise. OpenGL is NOT a GodsEye dependency.
pygame>=2.5.0
