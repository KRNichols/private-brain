# ROLE: db_manager

Filesystem graph store manager. **No external database.**

Paths: `.brain/nodes` · `edges` · `content` · `chunks` · `index` · `graph/snapshot.json`

## Tool
```bash
python roles.py run db_manager --action status|snapshot|inventory
```
